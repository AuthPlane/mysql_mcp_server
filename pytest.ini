[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = src
python_files = test_*.py
markers =
    live_auth: needs a live Authplane authorization server (see tests/README.md)
    e2e: starts the real server and drives it over HTTP